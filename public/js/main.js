(()=>{const h=document.querySelector('.site-header');const t=document.querySelector('.menu-toggle');const m=document.querySelector('.menu');const modal=document.getElementById('lead-modal');const modalClose=document.querySelector('.modal-close');const modalSource=document.getElementById('modal-source');const focusables='button,[href],input,select,textarea';window.addEventListener('scroll',()=>h.classList.toggle('scrolled',window.scrollY>40));t&&t.addEventListener('click',()=>{const open=m.classList.toggle('open');t.setAttribute('aria-expanded',String(open));});document.querySelectorAll('.menu a').forEach(a=>a.addEventListener('click',()=>{m.classList.remove('open');t.setAttribute('aria-expanded','false');}));document.querySelectorAll('[data-open-modal]').forEach(b=>b.addEventListener('click',()=>{modal.hidden=false;document.body.style.overflow='hidden';modalSource.value=b.getAttribute('data-open-modal')||'unknown';const first=modal.querySelector('input,button');first&&first.focus();}));const closeModal=()=>{modal.hidden=true;document.body.style.overflow='';};modalClose&&modalClose.addEventListener('click',closeModal);modal&&modal.addEventListener('click',e=>{if(e.target===modal)closeModal();});document.addEventListener('keydown',e=>{if(!modal.hidden&&e.key==='Escape')closeModal();if(!modal.hidden&&e.key==='Tab'){const f=[...modal.querySelectorAll(focusables)].filter(el=>!el.disabled);if(!f.length)return;const first=f[0],last=f[f.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus();}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus();}}});document.querySelectorAll('.acc-item').forEach(btn=>btn.addEventListener('click',()=>{const ex=btn.getAttribute('aria-expanded')==='true';btn.setAttribute('aria-expanded',String(!ex));}));const cookieBanner=document.getElementById('cookie-banner');const cookieLink=document.getElementById('cookie-link');const cookieAccept=document.getElementById('cookie-accept');if(!localStorage.getItem('cookieAccepted'))cookieBanner.hidden=false;cookieAccept&&cookieAccept.addEventListener('click',()=>{localStorage.setItem('cookieAccepted','yes');cookieBanner.hidden=true;});cookieLink&&cookieLink.addEventListener('click',e=>{e.preventDefault();cookieBanner.hidden=false;});if(window.lucide)window.lucide.createIcons();})();
